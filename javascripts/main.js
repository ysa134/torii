;(function($, window, document, undefined) {
	var $win = $(window);
	var $doc = $(document);

	$doc.ready(function() {
		// fullscreener
		$('.intro > img').fullscreener();
	});
})(jQuery, window, document);
